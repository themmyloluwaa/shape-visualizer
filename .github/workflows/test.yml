name: Docker CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run docker-compose stack
        uses: actions/checkout@v2
      - run: |
          echo "I am about starting out"
          echo "Successfully checked out of workflow directory"
          pwd
          npm i -g @cypress/react
          docker-compose -f testing.yml up --exit-code-from tests
